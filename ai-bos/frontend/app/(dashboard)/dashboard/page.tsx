// Placeholder — real overview cards, recent uploads, and recent chats
// are built in Step 3 (Dashboard UI).
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Overview cards, recent uploads, and recent chats will appear here.
      </p>
    </div>
  );
}
